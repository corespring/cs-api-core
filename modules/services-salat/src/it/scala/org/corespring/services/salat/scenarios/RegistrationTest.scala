package org.corespring.services.salat.scenarios

import org.corespring.services.salat.ServicesSalatIntegrationTest


class RegistrationTest extends ServicesSalatIntegrationTest {

  "registration" should {
    "create an org, user, content collection + apiClient" in pending
    "allow the new org to create an access token" in pending
    "allow the new org to insert content" in pending
  }

  "deregistration" should {
    "remove the org, all users, content collections + apiClients" in pending
    "remove all the items?" in pending
  }
}
