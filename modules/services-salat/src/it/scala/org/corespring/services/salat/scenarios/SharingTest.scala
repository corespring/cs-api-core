package org.corespring.services.salat.scenarios

import org.corespring.services.salat.ServicesSalatIntegrationTest

class SharingTest extends ServicesSalatIntegrationTest {

  "sharing" should {
    "allow an org to access an item that has been shared with them" in pending
    "not allow an org access to an item that is no longer shared with them" in pending
  }
}
